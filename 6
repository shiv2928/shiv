SHOW DATABASES;
CREATE DATABASE PR6;
USE PR6;
CREATE TABLE DEPARTMENT(DEPT_ID INT PRIMARY KEY, NAME VARCHAR(40));
CREATE TABLE STUDENT (ROLL_NO INT PRIMARY KEY,NAME VARCHAR(50),MARKS INT,ID INT,FOREIGN KEY (ID) REFERENCES DEPARTMENT (DEPT_ID));
INSERT INTO DEPARTMENT VALUES (1,"COMP");
INSERT INTO DEPARTMENT VALUES (2,"IT");
INSERT INTO DEPARTMENT VALUES (3,"ENTC");
INSERT INTO STUDENT VALUES (101,"ADAM",80,1);
INSERT INTO STUDENT VALUES (102,"BOB",70,1);
INSERT INTO STUDENT VALUES (103,"JACK",90,3);
INSERT INTO STUDENT VALUES (104,"EVE",95,2);
INSERT INTO STUDENT VALUES (105,"JULIE",88,2);
SELECT * FROM DEPARTMENT;
SELECT * FROM STUDENT;
SELECT * FROM STUDENT WHERE ID IN (SELECT DEPT_ID FROM DEPARTMENT WHERE NAME = "COMP");
SELECT * FROM STUDENT WHERE MARKS> (SELECT AVG(MARKS) FROM STUDENT);
SELECT NAME,MARKS FROM STUDENT WHERE MARKS> (SELECT MAX(MARKS) FROM STUDENT WHERE ID=1);
SELECT NAME,MARKS FROM STUDENT WHERE MARKS< (SELECT MAX(MARKS) FROM STUDENT);
SELECT MAX(MARKS) FROM STUDENT;


